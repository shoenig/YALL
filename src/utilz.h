/* Seth Hoenig 2011 */

#ifndef UTILZ_H
#define UTILZ_H

#include "stdlib.h"

size_t stlen(char* str);

int htoi(char* hex);

char* str_dup(char* str);

char* str_ezcat(char* stra, char* strb);

#endif
